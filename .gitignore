**/.DS_Store
**/.claude/
**/.vscode/
**/.ipynb_checkpoints
*.txt
*.log
*.pptx
CLAUDE.md

# Generated outputs. The GRAPE controls are expensive (~40 min each) and the
# figures are the record of those runs, so both are committed. The exported
# drive JSON is regenerated from the controls in seconds by export_jaqalpaw.jl.
results/*.json

# Scratch working area and slide material from the wider project; the sources
# this branch ships live at the repo root.
temp/
docs/
results/data/
results/figures/
results/logs/

# JaqalPaw: vendored upstream clone and its virtualenv, recreated by
# setup_jaqalpaw.sh
third_party/
.venv/
__pycache__/
